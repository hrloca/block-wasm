{
  "name": "blocks",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "serve": "webpack-dev-server",
    "build:wasm": "wasm-pack build ./wasm --scope hrloca",
    "watch": "chokidar './wasm/src/**/*.rs' -c 'npm run build:wasm'",
    "dev": "npm run build:wasm && npm run serve & npm run watch",
    "test": "cd ./wasm && cargo test"
  },
  "dependencies": {
    "chokidar-cli": "^3.0.0"
  },
  "devDependencies": {
    "webpack": "^4.25.1",
    "webpack-cli": "^3.1.2",
    "webpack-dev-server": "^3.1.10"
  }
}
